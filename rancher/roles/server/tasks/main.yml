# 格式化磁盘
- include: format-disk.yml

# 创建交换分区
- include: swap.yml

# 下载搭建rancher镜像
- name: RancherOS rancher/server
  docker:
    name: rancherserver
    image: rancher/server
    state: reloaded
    restart_policy: always
    ports:
    - "8080:8080"
